{% extends 'SMAdminBundle::layout.html.twig' %}
{%block header%}{{'admin.role.titleEdit'|trans({}, 'SMAdminBundle')}}{%endblock header%}
{% block body -%}
    <div class="row">
        {{ form_start(edit_form) }}
        <div class="col-md-8 col-md-offset-1">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title"><i class="fa fa-sitemap"></i> {{'admin.role.panelCreate'|trans({}, 'SMAdminBundle')}}</h3>
                </div>
                <div class="panel-body">
                    <div class="dataTable_wrapper">
                        <table class="table table-striped table-bordered table-hover">

                            <tbody>
                                <tr>
                                    <td>{{form_label(edit_form.name)}}</td>
                                    <td>{{form_widget(edit_form.name)}}</td>
                                </tr>
                                <tr>
                                    <td>{{form_label(edit_form.role)}}</td>
                                    <td>{{form_widget(edit_form.role)}}</td>
                                </tr>

                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="panel-footer">
                    {{form_widget(edit_form.submit, {'attr':{'class':'btn btn-primary'}})}}
                    <a href="{{ path('admin_role') }}" class="btn btn-warning">
                        {{'admin.button.backtolist'|trans({}, 'SMAdminBundle')}}
                    </a>
                </div>
            </div>
        </div>
        {{ form_end(edit_form) }}
    </div>
    {#    {{form_start(delete_form)}}
        {{ form_widget(delete_form.submit, {'attr':{'class':'btn btn-danger'}}) }}
        {{form_end(delete_form)}}#}
{% endblock %}
